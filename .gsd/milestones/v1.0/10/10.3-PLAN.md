---
phase: 10
plan: 3
wave: 2
---

# Plan 10.3: UI Model Indicator

## Objective
Enhance the React UI to display the active LLM processing the request so users have clear visibility into backend model swapping between the Base and Instruct models.

## Context
- frontend/src/components/ui-custom/NavBar.tsx (or App header)
- frontend/src/hooks/useHealthPoll.ts

## Tasks

<task type="auto">
  <name>Display Active LLM Badge in UI</name>
  <files>
    frontend/src/components/ui-custom/NavBar.tsx
    frontend/src/views/ChatView.tsx
  </files>
  <action>
    - Inject the output of `useHealthPoll()` into `NavBar.tsx` or `ChatView.tsx`.
    - Create a visible badge (e.g., `⚡ Model: {health.models.llm}`) at the top of the interface next to the generic title.
    - Style the badge to fit the pixel-art/glassmorphic aesthetics of the application.
  </action>
  <verify>Launch the frontend and verify the correct model name (Sarvam-1 / Llama-3.2) is shown depending on the `useRAG` toggle.</verify>
  <done>Frontend accurately displays the active backend LLM constraint.</done>
</task>

## Success Criteria
- [ ] Active Model indicator appears in the React interface.
